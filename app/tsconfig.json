{
  "extends": "../tsconfig.base",
  "compilerOptions": {
    "removeComments": true,
    // specify which module format to output
    "module": "commonjs"
    // // looks at all the files that get compiled and directs all output to a single file, making sure it's ordered properly
    // "outFile": "../js/app.js"
  },
  "files": [
    "./app.ts"
  ]
  // "include": [
  //   // look for all files ending in ts
  //   "./**/*"
  // ]
}